<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="MetaDataMapper">
    <resultMap id="TemplateResultMap"
               type="com.example.TaskFlow.model.response.RoleResponse">
        <result property="roleCode" column="role_code"/>
        <result property="nameKo" column="name_ko"/>
        <result property="description" column="description"/>
    </resultMap>

    <select id="findAllTemplate" resultMap="TemplateResultMap">
        SELECT
        position_code,
        name_ko,
        level,
        description
        FROM positions
        ORDER BY level ASC
    </select>

    <insert id="insertTemplate"
            parameterType="com.example.TaskFlow.model.request.template.CreateProjectTemplateRequest">
        INSERT INTO PROJECT_TEMPLATES (id, name, description, config_json, created_by)
        VALUES (#{id}, #{name}, #{description}, #{configJson}, #{createdBy}
    </insert>
</mapper>